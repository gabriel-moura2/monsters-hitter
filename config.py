SCREEN_WIDTH = 500
SCREEN_HEIGHT = 800
FPS = 60
FONT = "assets/fonts/MoonlitFlow-Regular.ttf"

MENU_BACKGROUND_COLOR = (200, 0, 200)

TITLE_DISPLAY_CONFIG = {
    "position": (int(SCREEN_WIDTH / 20), int(SCREEN_HEIGHT * 3 /10)),
    "text": "Monsters Hitter",
    "size": int(SCREEN_HEIGHT / 12),
    "color": (0, 0, 200)
}

START_DISPLAY_CONFIG = {
    "position": (int(SCREEN_WIDTH / 3), int(SCREEN_HEIGHT / 1.5)),
    "text": "Press space",
    "size": int(SCREEN_HEIGHT / 24),
    "color": (153, 221, 0)
}

LEVEL_BACKGROUND_COLOR = (0, 200, 0)